<%# Content Heading %>
<% if (title) { %>
    <h1 class="details-title"><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<%# A message %>
<%# messages() %>

<%# Inventory items display %>
<%- grid %>